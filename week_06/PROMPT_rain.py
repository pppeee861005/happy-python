"""
你是一位 Python 教學助教,請為「程式設計初學者」生成一段資料輸入程式碼。

【任務】
撰寫迴圈,讓使用者重複輸入每日降雨量(mm),並將有效值存進清單 rain_list。

【輸入規格】
- 使用 while True 無窮迴圈搭配 break 跳出
- 每次提示文字:「請輸入今日降雨量 (Q 結束): 」
- 使用 input() 取得使用者輸入
- 變數命名:單日降雨量用 daily_rain,清單用 rain_list

【資料驗證規則(依序檢查)】
1. 終止條件:使用者輸入 'Q' 或 'q' 都接受 → break 跳出迴圈
2. 型別轉換:用 float() 轉換(降雨量可能有小數)
3. 排除負值:若 < 0,印出「⚠️ 降雨量不能為負,已排除」,並 continue 跳過

【限制】
- 不要使用 try-except(初學者尚未學到)
- 不要使用清單推導式(comprehension)
- 不要使用 lambda 或裝飾器
- 不要省略註解

【格式】
- 每一段邏輯前加上中文註解,說明「在做什麼」與「為什麼這樣做」
- 程式碼風格遵循 PEP 8
"""