7/31
在voltr上練習裝docker/ 是一個容器平台。
docker裡面裝一個容器ollama。
在ollama裡面下載gemma4 2b來聊天。

# 7/31 Docker 與 Ollama 練習

  在 Vultr 的 Ubuntu 雲端主機上安裝 Docker。Docker 是用來建立及管理容器的容器平台。
在vultr的ubuntu雲端主機。上安裝docker。docker是用來管理及建立容器的容器平台。
  接著下載 `ollama/ollama` 映像，透過 Docker 建立並執行 Ollama 容器。
  接著下載ollama/ollama，映象，秀過docker建立並執行ollama容器。

  最後使用 Ollama 下載 `gemma4:e2b` 模型，並成功在 Vultr 主機上與模型聊天。
  最後使用ollama下載gema4:e2b。模形。並成功在vultr主機上與ai聊天真是高興。哈哈哈哈哈

  目前模型推理由 Vultr 主機的 CPU 與 RAM 執行，不需要呼叫第三方模型 API。這屬於自架雲端 LLM，還不是在個人電
  腦上執行的完全地端 LLM。
目前模形推理由 vultr主機的cpu與ram執行。
不要需呼叫第三方模形api。
這屬於自架雲端llm。
還是不是在個人電腦上執行的完全地端llm

8/1
今天在vultr上用GroQ聊天，聊的真開心。反應速度超快：一句話他0.8秒就回了，真是令人驚豔
