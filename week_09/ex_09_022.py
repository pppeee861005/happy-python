x = [1, 3, 5, 0, -1, 3, -2]
# 方法一：使用 list comprehension（推薦）  
result = [num for num in x if num >= 0]
result = [num for num in x if num >= 0]
result = [num for num in x if num >= 0]
result = [num for num in x if num >= 0]
result = [num for num in x if num >= 0]
result = [num for num in x if num >= 0]
print(result)  # [1, 3, 5, 0, 3]